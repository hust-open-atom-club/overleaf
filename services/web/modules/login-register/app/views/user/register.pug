extends ../../../../../app/views/layout-marketing

block vars
	- isWebsiteRedesign = false
	- forceLightMarketingNavbar = true

block content
	main#main-content.content.content-alt.auth-theme-page
		.container
			.row
				.col-lg-8.offset-lg-2.col-xl-6.offset-xl-3
					.card.login-register-surface-card
						.card-body
							.page-header.login-register-page-header
								h1.login-register-header-heading-focus(style='font-family: Merriweather, serif; margin-bottom: 12.5px !important;') #{translate("register")}
								.login-register-hr-text-container(style='margin-top: 1px; padding: 5px 0;')
								.notification.notification-type-info(style='margin-top: 18px; margin-bottom: 5px;' role='note')
									.notification-icon
										+material-symbol('info')
									.notification-content-and-cta
										.notification-content.text-left(style='font-family: Lato, sans-serif;')
											p Registration requires a Huazhong University of Science and Technology email address (*@hust.edu.cn).
											p We recommend using HUST CAS directly to log in.
											p If you still prefer to register with email, after registration and login, you can link your HUST CAS in "Account Settings" so that next time you can use "Log in with HUST CAS" to access this account.
											p(style='font-weight: bold; font-style: italic;') To keep everything under one account, please link your HUST CAS account in "Account Settings" instead of registering by email and later logging in separately with "Log in with HUST CAS", which would create a second account and make maintenance more difficult for us. Thank you for your understanding :).
							form(
								data-ol-async-form
								name='registerForm'
								action='/register'
								method='POST'
								aria-label=translate('create_account')
								style='margin-top: 0px !important;'
							)
								input(name='_csrf' type='hidden' value=csrfToken)
								.notification.notification-type-success.register-success-notification.mb-3(role='status' aria-live='polite' data-ol-sent hidden)
									.notification-icon
										+material-symbol('check_circle')
									.notification-content.text-left(style='font-family: Lato, sans-serif;')
										p Your account activation email has been sent. Please check your inbox and follow the instructions to complete activation.
										p If you don't see the email in your inbox, please check your spam folder first.
										p If you still can't locate it or encounter other issues, contact us at #[a(href='mailto:' + process.env.OVERLEAF_ADMIN_EMAIL style='color: var(--green-50);')= process.env.OVERLEAF_ADMIN_EMAIL].
										p(style='font-weight: bold; font-style: italic;') The activation link will expire in one week, after which you'll need to register again.
								div(data-ol-not-sent)
									+formMessages()
								if err_message
									.notification.notification-type-error.mb-3(style='margin-top: 20px;' role='alert' aria-live='assertive')
										.notification-icon
											+material-symbol('error')
										.notification-content.text-left(style='font-family: Lato, sans-serif;')
											p= err_message
								+customFormMessageNewStyle('invalid-password-retry-or-reset', 'danger')
									| !{translate('email_or_password_wrong_try_again_or_reset', {}, [{ name: 'a', attrs: { href: '/user/password/reset', class: 'auth-theme-password-reset-link', 'aria-describedby': 'resetPasswordDescription' } }])}
									span.visually-hidden(id='resetPasswordDescription')
										| #{translate('reset_password_link')}
								+customFormMessageNewStyle('password-compromised')
									| !{translate('password_compromised_try_again_or_use_known_device_or_reset', {}, [{name: 'a', attrs: {href: 'https://haveibeenpwned.com/passwords', rel: 'noopener noreferrer', target: '_blank'}}, {name: 'a', attrs: {href: '/user/password/reset', target: '_blank'}}])}.
								.form-group
									label(for='emailField' style='font-family: Lato, sans-serif; margin-bottom: 0.5em;') #{translate("email")}
									input.form-control(
										aria-label='email'
										type='email'
										name='email'
										placeholder='email@hust.edu.cn'
										required
										autocomplete='username'
										autofocus
										style='font-family: Lato, sans-serif !important; height: 45px !important; border-radius: 20px !important; padding: 10px 15px !important; border: 1px solid var(--border-primary-themed) !important; background-color: var(--bg-tertiary-themed) !important; color: var(--content-primary-themed) !important;'
									)
								if showPasswordField
									.form-group
										label(for='passwordField' style='font-family: Lato, sans-serif; margin-bottom: 0.5em;') #{translate("password")}
										input.form-control(
											aria-label='password'
											type='password'
											name='password'
											placeholder=translate('password')
											required
											autocomplete='new-password'
											style='font-family: Lato, sans-serif !important; height: 45px !important; border-radius: 20px !important; padding: 10px 15px !important; border: 1px solid var(--border-primary-themed) !important; background-color: var(--bg-tertiary-themed) !important; color: var(--content-primary-themed) !important;'
										)
								if showInviteCodeField
									.form-group
										label(for='inviteCodeField' style='font-family: Lato, sans-serif; margin-bottom: 0.5em;') Invitation Code
										input.form-control(
											aria-label='invite-code'
											type='text'
											name='inviteCode'
											placeholder='Invitation Code'
											required
											autocomplete='off'
											style='font-family: Lato, sans-serif !important; height: 45px !important; border-radius: 20px !important; padding: 10px 15px !important; border: 1px solid var(--border-primary-themed) !important; background-color: var(--bg-tertiary-themed) !important; color: var(--content-primary-themed) !important;'
										)
								.actions
									button.btn.btn-primary.w-100(
										type='submit'
										data-ol-disabled-inflight
										style='font-family: Lato, sans-serif; padding: 10px 25px; border-radius: 20px;'
									)
										span(data-ol-inflight='idle') #{translate("create_account")}
										span(hidden data-ol-inflight='pending') #{translate("registering")}…
							if (settings.ldap && settings.ldap.enable) || (settings.saml && settings.saml.enable) || (settings.oidc && settings.oidc.enable)
								p.ciam-login-register-or-text-container
									span.ciam-login-register-or-text #{translate("or")}
							if settings.ldap && settings.ldap.enable
								.actions(style='margin-top: 15px;')
									a.button.btn-secondary.btn(
										href='/ldap/login'
										style='width: 100%;'
										data-ol-disabled-inflight
									)
										span(data-ol-inflight='idle') #{settings.ldap.identityServiceName}
										span(hidden data-ol-inflight='pending') #{translate("logging_in")}…
							if settings.saml && settings.saml.enable
								.actions(style='margin-top: 15px;')
									a.button.btn-secondary.btn(
										href='/saml/login'
										style='width: 100%;'
										data-ol-disabled-inflight
									)
										span(data-ol-inflight='idle') #{settings.saml.identityServiceName}
										span(hidden data-ol-inflight='pending') #{translate("logging_in")}…
							if settings.oidc && settings.oidc.enable
								.actions(style='margin-top: 15px;')
									a.button.btn-secondary.btn(
										href='/oidc/login'
										style='width: 100%;'
										data-ol-disabled-inflight
									)
										span(data-ol-inflight='idle') #{settings.oidc.identityServiceName}
										span(hidden data-ol-inflight='pending') #{translate("logging_in")}…

							.login-register-hr-text-container
							.ciam-register-login-link
								.login-register-other-links
									.p
										| #{translate("already_have_sl_account")} 
										a(href='/login') #{translate("login")}
